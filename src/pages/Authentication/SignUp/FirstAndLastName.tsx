import React from "react"
import { Box } from "../../../theme"

interface FirstAndLastNameProps {}
const FirstAndLastName = ({}: FirstAndLastNameProps) => {
	return <Box />
}

export default FirstAndLastName
